
class Constants {
  Constants._();

  /// used for (app folder, send to server)
  static const appName = 'Brandfit';
  /// used for (app title)
  static String appTitle = 'Brandfit';
  static String appVersionName = '0.1.3'; //match by pubspect.yaml
  static int appVersionCode = (0 * 10000) + (1 * 100) + (3);
}
